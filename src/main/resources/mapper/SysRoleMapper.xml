<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.base.framework.admin.mapper.SysRoleMapper">
    <insert id="save" parameterType="com.base.framework.admin.model.entity.SysRoleEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role(id, name, code, created_by, is_active) VALUES(#{id}, #{name}, #{code}, #{createdBy}, #{isActive})
    </insert>

    <select id="getDetailByCode" resultType="com.base.framework.admin.model.entity.SysRoleEntity">
        SELECT * FROM sys_role WHERE code = #{code}
    </select>

    <select id="getDetailById" resultType="com.base.framework.admin.model.entity.SysRoleEntity">
        SELECT * FROM sys_role WHERE id = #{id}
    </select>

    <update id="deleteById">
        DELETE FROM sys_role WHERE id = #{id}
    </update>

    <select id="queryUserList" resultType="com.base.framework.admin.model.entity.SysRoleEntity">
        SELECT id, name, code, is_active AS isActive, created_at AS createdAt, created_by AS createdBy FROM sys_role
        <where>
            <if test="isActive != null and isActive != ''">
                AND is_active = #{isActive}
            </if>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
    </select>
</mapper>
